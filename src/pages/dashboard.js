import React from 'react';

const Dashboard = () => (
  <div className="p-4">
    <h1>Dashboard</h1>
    <p>Only accessible to users with "view_dashboard" permission.</p>
  </div>
);

export default Dashboard;
